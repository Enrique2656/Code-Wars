Description: 
// Simple, remove the spaces from the string, then return the resultant string.

function noSpace(x){
    return x.split(' ').join('');
}
  // removes spaces from string then return string without space.
  // any commas? - no
  // [a b c]
  // [abc]